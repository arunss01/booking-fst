/** @type {import('next').NextConfig} */
const nextConfig = {
    // Kosongkan konfigurasi di sini.
    // Jika Anda ingin menggunakan reactCompiler: true, pindahkan ke sini.
};

module.exports = nextConfig;